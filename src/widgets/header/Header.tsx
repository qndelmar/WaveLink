import React, {FC} from 'react';
import './header.scss'

const Header:FC = () => {
    return (
        <header className="header">
            <p>Basic header</p>
        </header>
    );
};

export default Header;